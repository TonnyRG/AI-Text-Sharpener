# Studio 0.4.0 本机验证记录

日期：2026-09-18。环境：Fedora Linux 44、Python 3.12.14、CPU、RapidOCR 3.9.2、ONNX Runtime 1.30.0。原仓库基线：`3c33ff1`。

## 自动验证

运行 `.venv/bin/python -m pytest -q`：**89 passed, 5 skipped**（约 4.5 秒）。跳过项是旧测试所需本地样本/条件未满足，不能计为通过。`pip check` 通过。

新增回归覆盖：

- 使用真实字形渲染的模糊样本，恢复字体、字号及坐标，位置误差约束 3 px；候选仅为有限字体集。
- 字体真实宽度测量，窄字与宽字不再按同一估计宽度排版。
- 中文本地化字体名称仍能匹配英文家族别名。
- 纯黑背景采样与旧版擦除羽化边缘。
- 未选择区域的像素保持、空白区域失败提示。
- SVG 路径、PPTX SVG 引用及 PNG 后备图结构。
- 纯图片 PPTX 无损提取、拒绝组合幻灯片。
- HTTP 导入、保存修订冲突、写操作令牌及路径检查。

## 真实 OCR 与界面流程

用本机生成的 1600×900 中英文模糊文字测试页进行完整识别，检测出 8 处文字，随后完成逐行候选字体拟合。测试页包含白底、深色底、中英文和不同字号。示例由字体渲染后进行高斯模糊，因此更适合验证几何恢复，不能替代真实生成式 AI 样本评估。

独立标题试验：原字号 78 px、位置 (90,95)，拟合字号 78.14 px、位置 (89.56,94.38)，候选恢复为同款 Microsoft YaHei Bold。该数据仅对应这一合成样本，不能解读为普遍误差上限或“98.9% 还原率”。

在本机浏览器实际验证了：示例导入、识别匹配、结果显示、X 坐标微调、自动保存、撤销、PPTX 导出和服务重启后项目恢复。浏览器没有捕获到警告或错误。预览与导出使用相同的字体轮廓。

## PPTX 验证

从实际应用导出示例 PPTX，Office XML 验证器返回 `All validations PASSED!`。使用本机 LibreOffice headless 打开并转换为 PDF，随后渲染为 PNG 目视检查，文字、位置及背景正常。

验证不等于所有演示软件都支持 SVG。本机没有使用 Microsoft PowerPoint 或 WPS 做兼容性测试。PPTX 保留 SVG 轮廓和无损 PNG 后备图；原生文本框编辑尚未实现。

## 待真实样本验证的部分

- AI 字形失真、多字体混排、艺术字、倾斜和透视。
- 复杂纹理背景上的擦字修复、文字与图形交叠。
- 候选字体不含原字体时的视觉接近程度。
- 低对比度及极小文字的 OCR 召回率。

现有流程对低置信度、明显倾斜或无可用字体的区域保留原图，并提供手动画框、字体候选和手动校正。尚未收到用户的真实问题样本，不能声称已实现所有输入的近乎完美重绘。


## 整份 PPT 批量处理增量验证

新增批量识别、逐页保存、取消续做、失败页重试、保护手动修改，以及整份导出入口与逐页进度。回归测试结果：**92 passed, 5 skipped**。三项新测试覆盖多页导出、失败继续与重试、取消后的恢复。

另用两页纯图片 PPTX 实际运行本机 OCR（中文「中文字体测试」、英文「Batch export 2026」），两页识别和字体拟合均完成。导出的两页 PPTX 通过 Office XML 校验，并用 LibreOffice 转为 PDF 后逐页检查，顺序与内容正确。浏览器已验证批量按钮可调用任务，已有页面不会重复识别。

## 功能范围调整

已移除仅用于文字校对的 DeepSeek 接入，包括设置、云端请求和建议界面。该功能未实现原图与重绘图的位置、字号、字重和字形的精确对比，不符合本工具的主要目标。本地字形拟合、手动微调和整份 PPT 批量处理保持可用。


## 字重还原改进

用户提供的「研究生新生见面会」样本定位到候选裁剪问题：初选每家族三种字重，漏掉 Source Han Sans CN Regular 和 Medium。新增前三个候选家族的完整直立字重复核。该样本由 Normal 33.48 px 改为 Regular 33.34 px，位置仍为 (111,174)。原拟合分数 0.9663，更新后 0.9704。按算法提取的软笔画掩膜估计，渲染笔画总覆盖量/原图掩膜覆盖量由 0.9267 变为 1.0207；这不是通用字重准确率，只用于这个样本的诊断。

同时新增 0–12 px 矢量描边细调，默认 0、步长 0.05；不改变字符进位。新增回归验证漏选 Regular 的修复、笔画量增加、参数校验以及 SVG/PPTX 的参数保留。测试结果 95 passed, 5 skipped。带 0.4 px 描边的 PPTX 经 XML 校验及 LibreOffice 打开转换验证。用户该行已重新匹配，原项目配置已另存本地 audit/weight-refinement-backup/project-before.json。


## 全量重新识别

批量入口新增「重新识别全部页面」与「仅处理未完成页面」。全量重扫在执行前保存配置备份，逐页完成后替换旧结果；失败或取消的页面保留原结果及待重扫标记，支持继续未完成页面。新增回归覆盖已处理/锁定区域重扫、配置备份，以及取消/失败后续做。测试结果 98 passed, 5 skipped；JavaScript 语法检查通过。

## 字符范围校正与编辑预览

纯生图测试集第 02 页的整行检测框包含标题旁竖条，但 RapidOCR 的逐字位置从 x≈111 开始，竖条位于 x≈72。现启用 return_word_box，并在文字/置信度完整一致时，以字符位置和完整连通笔画校正原字范围。该标题框由 [55,54,601,83] 校正为 [112,64,536,63]；不是针对红色或矩形的保护规则。识别定位缺失、文字不全或明显不一致时保持原范围。

在原始 6 页上运行实际 OCR：第 02 页标题、第 03 页标题和第 05 页“实施”范围被校正，其余行保持原范围。针对第 02 页标题重新拟合和渲染，竖条区域与原图逐像素一致；标题以下像素与用户认可导出一致。该页标题的字体拟合仍有差异，范围校正不等于字体质量已完全解决。诊断与原配置保存在本机 audit/decorative-bar-fix，不覆盖用户项目或认可基线。

新增无持久化副作用的 /api/preview，接收当前编辑快照，复用 compose_page 与导出相同的 SVG 生成函数。前端停止编辑约 450 ms 后请求预览，连续编辑只展示最新快照，预览不清空撤销历史。保存编辑时使旧文件预览失效。空字符串仍擦除原字；删除区域恢复原图。

OCR 框增加 X/Y/宽/高输入及八个边角拖动手柄，框与重绘文字坐标独立。在独立副本浏览器中验证了改字、改 X、改框宽、拖动右下角、清空文字和撤销；预览显示为最新生成 SVG，未触发 PPT 导出。框宽从 680 拖至约 777、框高从 109 拖至约 159 后，文字 x=580、y=220.53 保持不变。

本次测试结果：105 passed, 5 skipped；JavaScript 语法检查通过。新增回归含真实生成标题裁片、不同颜色的竖条和同色圆形排除、彩色已识别字符保留、缺失字符定位时不收紧、预览与导出 SVG 一致且不写项目文件、清空与删除的区别。不能据此承诺能分离所有与文字相连或重叠的图形。

## 2026-09-19：公式专用流程

- 加入本地 MFD 1.5 ONNX 公式检测、独立环境中的 RapidLaTeXOCR 0.0.9、MathJax 3.2.2 路径排版。只在安装时下载组件，推理与预览不联网。
- 普通 OCR 碎片合并为完整公式；超过一半的重叠才参与合并，避免模型框边缘吸收邻近标题。OCR 对齐范围补充检测框，修复被截掉的前导符号。
- 公式默认保留原图。保留区域在合成末尾恢复原始像素，阻止其他文字的擦除或移动覆盖它。公式 LaTeX 修改、匹配位置、再次识别、SVG/PPT 导出已接入。
- 普通文字自动拟合得分 < 0.35 或越出原框及容差会停用；旧项目导出同样过滤。手动编辑不受这一自动拟合限制。
- 测试材料：v2 6 页、24 个公式；原始预测和项目快照位于 `audit/formula-next`。识别流程不读取测试集参考 LaTeX，没有更新模型权重。该测试集已经用于开发与人工检查，不再视为独立盲测集。
- v1 原有 85 个启用文字区域没有被新增拟合回退误停用，6 页均未检出公式。此项是回归检查，不代表所有普通 PPT 都不会误检。
- 单测覆盖：零分拟合、越界拟合、旧项目原像素回退、普通文字手动放大、完整公式合并、邻近标题边缘重叠、其他文字覆盖保留公式、缺失文本字体的公式验证、分数/矩阵/分段矢量渲染、非法 LaTeX 不覆盖已有文件、实际笔画加粗。
- 浏览器在 8767 的隔离副本验证：勾选公式、修改 LaTeX 后即时显示新增项、重新识别恢复原表达式并取消启用；控制台无错误。主用户项目未参与 UI 测试。
- 限制：数学字体尚未与原图字体逐字匹配；化学符号、某些分段结构和深色背景公式仍有识别错误。背景补全沿用原方法，深色渐变可能出现修补痕迹。内容识别与字形保真是两项不同的评估。

## 2026-09-19：Ctrl+Z 与重做

- Ctrl+Z 在文字、LaTeX、字号和坐标等属性输入框内也由应用处理，避免被输入框过滤或与浏览器原生撤销冲突；Ctrl+Shift+Z / Ctrl+Y 重做。
- 每页保存 80 步内存历史。连续输入按同一字段及 1 秒间隔合并，撤销恢复所选区域；删除后可恢复并重新选中该区域。
- 切换页、自动保存、预览和导出不清空历史。匹配/识别任务的区域变化记入相应页面的历史；后台只更新轮廓尺寸不增加虚假撤销步骤。
- 撤销后仍走原有自动保存及预览流程。切换项目、刷新或关闭窗口会清空历史；不撤销文件导入、外部文件下载或后台扫描完成状态。
- 在 8767 的隔离项目副本实测：文字输入框 Ctrl+Z、Ctrl+Shift+Z、字号、OCR 框宽、删除恢复、Ctrl+Y、切换页面后撤销、公式重新识别后恢复原 LaTeX 与启用状态、导出整个 PPT 后继续撤销、连续输入 abc 一步撤销。浏览器控制台无错误，JavaScript 语法检查通过，8766 服务已读取新版静态资源。未改动用户原项目。

## 2026-09-19：统一导出与属性面板分组

- 将「更多导出 / 导出整个 PPT」统一为「导出…」：页面范围（当前页、全部页面）与格式（PPTX、SVG、PNG）独立选择。全部页面 SVG/PNG 按顺序命名为 001、002…并打包 ZIP；当前页下载单文件，当前页 PPTX 仅含所选页。JSON 配置备份单列。
- 右侧按内容、字体与样式、文字位置、原字范围与背景分组，可折叠；常用编辑默认展开，字体候选和长说明按需展开。主匹配按钮、次级匹配按钮、删除区域分层。公式隐藏字体选择与普通文字匹配控件。OCR 框调整按钮显示进入/完成状态，折叠该组会退出范围编辑。
- 新增 10 项回归，覆盖两种范围 × 三种格式的真实渲染、HTTP 下载、页序、最新修改、仅更新所选页的渲染状态；无效范围/格式/页面拒绝；取消 ZIP 导出保留先前文件并清理临时包。结果：127 passed, 5 skipped。JavaScript 语法检查及 git diff --check 通过。
- 8767 独立两页项目实测全部页面 SVG/PNG ZIP、当前页 PPTX 导出，文字内容 Ctrl+Z、OCR 框进入/退出、公式面板切换；浏览器无警告或错误。1280、1024、800 像素宽度检查，未见横向溢出；单页 PPTX 通过 Office XML 校验。未修改用户原项目。
- 8766 服务在确认无运行任务后重启，载入新导出接口。已有页面刷新后使用新版界面。

## 2026-09-19：直接拖动、缩放文字与原图对照

- 已处理页默认进入重绘预览。绿色框内拖动移动文字，四角等比缩放；字号、字距、笔画共同变化，原字 bbox 保持不变。拖动采用同一份矢量预览的区域变换，松开后重新合成并自动保存，避免只移动框而文字不动。
- 默认不显示橙色框；仅在高级「原字擦除范围」中启用。原图模式显示原位置的框并仅供选择；锁定、未启用或没有实际重绘轮廓的区域不提供缩放手柄。手柄大小随画布缩放保持 10 个屏幕像素。
- 新增「按住看原图」，松开/取消/失焦返回，支持空格和回车；临时对照隐藏文字框，不更改选择、编辑数据或滑动对比位置。滑动对比继续使用当前实时重绘图。
- Node 7 项几何测试覆盖四角对边固定、等比例、字号/笔画/字距上限与反向拖动下限，已加入 CI。Python 129 passed, 5 skipped；新增真实文字/公式比例验证与背景擦除范围不变、SVG 区域标记转义检查。JavaScript 语法检查及 git diff --check 通过。
- 8767 隔离项目实测：标题右下角缩放（字号 78.51 → 94.395），框内移动（x/y 89.16/95.31 → 186.5/241.5），bbox 始终为 67/72/691/119；逐步撤销恢复原值。公式缩放（78.51 → 102.613）后导出单页 PPTX，通过 Office XML 校验，嵌入 SVG 与保存配置重新生成的预览完全一致。浏览器中核对了滑动对比及对照按钮释放后恢复状态。
- 8766 服务在空闲时重启加载更新；用户原项目未作为测试对象修改。

## 2026-09-19：工作区管理与导出路径

- 左侧新增工作区管理，列出工作区及页数，支持删除至应用回收站、恢复。后台校验版本与任务空闲状态；删除后旧窗口不能通过保存重新创建该工作区。当前工作区删除后切换剩余项目，无剩余则清空编辑状态与撤销历史，回到导入界面。原始导入文件不受影响。
- 导出窗口新增保存文件夹浏览器、完整路径输入、文件名。范围与格式仍独立；补齐扩展名，同名文件原子增加编号，不替换已有文件。支持不提供硬链接的文件系统回退；导出失败清理临时副本，成功后展示实际保存路径并记住目录。JSON 编辑数据备份使用同一路径选择。
- 新增 21 项后端验证：删除恢复完整性、过期版本与处理中删除拒绝、令牌验证、六种导出组合和 JSON 指定路径保存、同名保护、路径/扩展名校验、工作区数据目录保护、文件系统回退、取消与权限失败。Python：150 passed, 5 skipped；Node：7 passed。JavaScript 语法及 git diff --check 通过。
- 8767 隔离工作区实测删除唯一项目后的空状态、刷新后从回收站恢复、逐层选择文件夹、自定义中文文件名、重复导出自动编号、JSON 保存、刷新记住目录、无扩展名的批量 PNG 自动保存 ZIP。PPTX 通过 Office XML 校验。测试仅操作 audit/interface-export 的独立数据与输出，没有删除用户真实工作区。

## 2026-09-20：集中复核、保留原图与多色文字

- 新增整份项目的复核队列，按页面列出拟合较弱、识别不确定、异常字距、窄体／斜体、区域相碰与越界提示。支持跨页定位滑动对比、确认无误、单处保留与本页批量保留；确认状态保存后重开仍有效，内容／样式／相碰关系变化后重新检查。每页撤销历史涵盖复核操作，复核窗口内也支持 Ctrl+Z。
- 显式保留原图的普通区域与公式一样，在最后恢复完整源图裁片，避免被相邻擦除或移动文字覆盖。原区域数据仍保留，重新开启重绘可继续编辑。
- 增加逐字符颜色片段，复用同一 HarfBuzz 字形与坐标；填色和笔画颜色同步，不以重新排版换取多色。新识别／拟合自动尝试提取颜色；已有项目可单区域、当前页或全部页恢复颜色。批处理保留锁定、停用和手动配色，低拟合、内容不对应、只有灰度变化或证据不足时不猜测。
- 右侧支持逐片段改色、选中文字着色和整行统一颜色。文字改动清除旧颜色索引，移动／缩放保留片段。取消整份颜色任务不写入部分项目；预览、PNG、SVG 和 PPTX 使用相同轮廓。
- Python 全套 162 passed, 5 skipped；Node 17 passed。新增测试覆盖强调色提取、单色不误分色、坐标与描边不变、无效片段拒绝、内容变化防止错位套色、跨导出一致性、相邻区域原图保护、批量颜色任务跳过锁定／手动区域、取消不写入、复核状态失效及保存、Unicode 索引和片段覆盖。JS 语法与 diff whitespace 检查通过。
- 8767 独立工作区实测：复核计数 47 → 确认后 46 → 撤销 47；本页批量保留后 42 → 撤销 47；跨页定位后单处保留／撤销；4 页颜色恢复得到 3 处双色结论，浏览器显示强调色与片段编辑；按页撤销／重做、选中文字着色及保存后重开已验证。个人样例和生成图只保存在本地 audit，没有改动用户实际项目或上传样例文件。
- 限制：自动分色面向纯色文字片段，不还原渐变／浮雕。标点附近和字形不匹配处仍可能分色偏移，需对照原图或手动修正。复核提示是启发式提示，不代表 OCR 正确率。


## 2026-09-20 — 整数调整与旋转文字

- 原字方向（source_rotation/source_quad）与输出角度（rotation）分别保存，后者绕未旋转字形框中心旋转。擦除、自动拟合和提取多色使用前者。
- 旋转行局部校正后匹配，再将候选的位置映射回页面。导出保持矢量轮廓，预览共用同一变换。
- 自动拟合在相似度相差不超过 0.01 时优先整数，手动字号、字距和缩放取整数。旧结果可单处取整并撤销。
- 合成测试覆盖 -25°、27°、±90°、180° 的拟合与位置，旋转擦除避开相邻装饰、保存和 PPTX 内 SVG 角度、异常角度拒绝、旋转缩放固定对角、吸附、相碰及整数舍入。
- 本机真实 OCR 合成页识别中文 24.29°、英文 -19.54° 和竖向英文 90.65°（目标分别 24°、-20°、90°）；三组拟合分数约 0.982、0.970、0.965。此为功能回归样例，不是通用准确率评估。
- 限制：整行刚性旋转；不承诺曲线文字、透视变形或逐字直立的传统竖排。方向或内容错误仍可校正或保留原图。


## 2026-09-20 — 混排恢复、自动取整与框选

- 公式检测不再丢弃被替换行的 OCR 文本；中英混排且没有数学运算符的高置信行，在覆盖检测区域时保留普通文字。含等式的混排仍保留公式检测。
- 切换普通文字作为可撤销后台操作，复用原文；旧区域无文字时局部 OCR，失败不保存转换。完成后自动匹配并预览。
- 匹配只比较整数候选；旧工作区打开、预览与保存校验均自动取整，旧分数失效。移除了需要点击的取整按钮。相较上一节，已取消保留小数的例外。
- 空白点击取消选择；从画布及周围空白拖动框选，按完整包含判断，支持反向拖动、旋转框、原图模式和保留原图区域；Shift/Ctrl 点击增减。
- 多选支持整体移动、对齐吸附、方向键、删除及撤销；属性区显示选择数量，避免批量时误改某一个对象。锁定区域不移动、不删除。
- 回归覆盖混排/等式区分、OCR 原文保存、旧公式重识别和失败保留、转换后台任务、旧项目与预览取整、旋转框框选。
- 验证结果：后端全量 200 passed / 5 skipped，前端 32 项通过。真实 OCR 对旧的空文本公式框补识别成功；界面验证自动取整、空白取消、多选三框、整体移动、删除/撤销、公式转换及撤销/重做。局部 OCR 增加 20 px 边缘上下文，避免紧贴文字的裁剪被检测器忽略。

## 2026-09-20 — 多选批量字体与样式

- 多选属性区提供字体／字重、字号、字距、笔画加粗与颜色；不同值显示「多种」，只统一明确操作的属性。控件展示参与此次修改的区域值，跳过锁定或保留原图区域并显示数量。
- 数学区域允许改字号、笔画和颜色，不改变数学字体与字距。其他样式操作保留片段颜色，显式改颜色则统一整行。
- 批量换字体先对候选页面生成预览，缺字或排版失败时不改任何区域。修改共用自动保存／预览／导出及历史记录；多选输入框内 Ctrl+Z 可一次撤销整批，并恢复多选。
- 前端 41 项通过（新增 9 项批量规则回归），相关后端 40 项通过。界面验证多种值、两区域同改整数字号、未选区域不变、Ctrl+Z/重做、字体字重、字距、笔画、颜色、缺字字体拒绝、锁定跳过。

## 2026-09-21 — 精简界面说明

- 移除顶部「本地运行」、画布常驻操作说明、侧栏及属性区重复说明；操作指引集中于悬停提示和使用说明。
- 保留保存／预览状态、实际复核原因、批量跳过数量和重新识别覆盖提醒；常规完成通知缩短，空状态不占额外行。
- 验证：JavaScript 语法检查、HTML 元素 ID 完整性、浏览器单选／多选及空提示收起正常，无脚本错误。

## 2026-09-21 — 邻行干扰、单字方向与字重

- 普通文字拟合及擦除使用相同的单行掩膜：仅排除紧贴裁剪上下边缘、与主行有明显间隔且面积很小的残片。内部分离笔画、重音、标点保留，公式继续使用原有证据提取。
- 对 OCR 给出非零角度的单字符，同时拟合水平和原角度；分数接近时采用水平，真实倾斜／±90°／180° 保留。修正后的原字角度用于擦除，原 OCR 四角不变，旧角度另存 ocr_rotation。单字背景采用主要底色，排除与中心字形分离的边角残片，避免圆形编号的外侧背景污染字高。
- 普通字体候选在轮廓重合度之外比较软掩膜墨迹量，对偏粗和偏细对称扣分，不固定偏好某种字重；字号、字距保持整数。
- 新增 16 项合成图像回归：邻行上下残片不影响字号及擦除、保留点和重音、误报 -4°／14°／90° 的水平数字、真实单字 -25°／14°／±90°／180°、圆形底色边角、不同模糊程度的 Regular/Bold。完整后端结果为 216 passed / 5 skipped。
- 本地真实样例单独重算 4 页的 11 个区域并检查实际合成图：英文标题不再因上一行残片而放大压紧，普通表格行恢复较轻字重，两组编号不再出现异常倾斜和超出圆形的数字。局部样例、对比图和项目数据只留在被忽略的 audit 目录，未覆盖用户工作区或输入文件。
- 限制：邻行分离、单字背景和评分仍为图像启发式；笔画相连、复杂背景或找不到相近字体时仍需人工复核。已有结果需用户重新匹配／识别，不自动覆盖编辑。

## 2026-09-21 — GitHub Actions 字体依赖

- 远端最近两次运行的 Python 3.11 / 3.12 均在单元测试阶段失败，依赖安装和浏览器脚本检查通过。公开 API 可读状态及注释，历史详细日志需要登录，未将退出码误当成具体断言结果。
- 新建独立 Python 3.12 环境，仅启用 Liberation 字体并隔离本机公式资产，复现 2 failed / 207 passed / 12 skipped：普通文字恢复的两项中英混排测试均报缺少中文字库。补充系统 Noto CJK 后相同环境为 209 passed / 12 skipped。跳过项为已有的个人图片、可选公式运行时和特定字重字库测试，没有通过跳过失败用例解决问题。
- CI 显式安装 fontconfig、Liberation 和 Noto CJK，并在测试前验证中英混排覆盖。pytest 输出 JUnit；后续步骤将失败用例及错误信息写入运行摘要和注释，便于定位。完整测试集合、失败退出码和两版本矩阵均保留。
- AGENTS.md 增加每次推送后检查对应提交的远端测试结果要求，区分本机测试通过、推送成功与 CI 通过。

## 2026-09-22 — 文字／公式综合判定、局部复查与双候选比较

- 增加独立 classification 模块，综合词法、OCR 置信度及图像中的分式／上下标结构。公式检测框是候选，不直接决定区域类型；保留普通 OCR 原文及自动判定依据。
- 可疑裁片带上下文放大重读；检测器漏行时，仅对已知单行使用纯识别补读。坐标映射回原页，排除裁片上下文里的其他行；局部公式边界只允许小幅扩展，并检查邻行。整页 OCR 和局部 OCR 分别拟合，防止复查结果覆盖更好的原识别。
- 普通字体与数学排版在完全相同的原图裁片、缩放和坐标上比较，不各自对齐。评分包括轮廓重合及墨迹量；设置绝对质量门槛与胜出差值，两种低质量结果不会仅因相对胜出而自动采用。可疑区域保留原图并进入集中复核，接受、保留和撤销沿用现有操作；人工转换普通文字清除旧自动结论。
- 34 项分类回归覆盖缩写／单位／混排、分式和上下标、局部坐标与单行补读、邻行保护、同画布比较、缺字与偏移、两者接近的保留策略、无普通 OCR 的公式识别和取消。前端增加保留状态的待复核区域、确认与手动修改检查。
- 完整本机验证：Python 250 passed / 5 skipped，前端 43 项通过；服务重启后浏览器首页正常加载。
- 本地真实样例：普通汇报第 2、7、9 页，以及公式测试集第 1、3 页。LC-MS、MS/MS 混排保持普通文字；页码 07/12 从公式候选恢复普通文字。7 个公式区域继续保留公式类型，没有强行压成普通行；其中二次识别仍存在字符或结构错误，拟合未达门槛，全部保持原图待核对。单页检测及候选比较约 3–30 秒（本机少量样例，不是性能基准）。个人样例与诊断输出仅保存在本地 audit，未覆盖原工作区。
- 限制：图像结构规则仍是启发式；字形匹配分数不是内容正确率，不能据此宣称通用准确率提高。当前未微调模型，未自动启用公式，也不保证矩阵／分段表达式的 LaTeX 正确。

## 2026-09-22 — 旋转角度吸附

- 拖动旋转手柄时，在常用 45° 倍数或其他已启用文字／公式角度的 3° 内自动吸附，取最近目标；±180° 接缝按圆周距离计算。使用现有「对齐吸附」开关；Shift 保持 15° 步进，Alt 临时绕过所有吸附。修饰键在鼠标停住时也即时生效。
- 旋转时显示角度，吸附时显示紫色参考线；提示只存在于画布拖动期间。保留原有单区域旋转、保存和一次撤销流程，不改变原字擦除范围、位置或字号。
- 前端 48 项通过，新增自动吸附／脱离阈值、其他文字与公式角度、跨接缝、无效目标过滤、禁用与 Shift 优先、旋转快照不变检查；JavaScript 语法及 diff 检查通过。
- 独立工作区浏览器验证：同一拖动开启吸附时为 90°，关闭时为 89°；Ctrl+Z 一次回到 0°。没有改动用户原工作区。
